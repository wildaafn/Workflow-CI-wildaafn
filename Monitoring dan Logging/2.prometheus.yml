global:
  scrape_interval: 15s

rule_files:
  - "prometheus_alert_rules.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["localhost:9093"]

scrape_configs:
  - job_name: "ml_model"
    static_configs:
      - targets: ["localhost:8000"]
